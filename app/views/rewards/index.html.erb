<h1>Rewards Index</h1>
<ul>
  <% @rewards.each do |reward| %>
    <li><%= link_to reward.title, reward %> <%= image_tag reward.image.url, height: '300' if reward.image.exists? %></li>
  <% end %>
</ul>
