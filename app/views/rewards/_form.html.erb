<%= form_for @reward do |f| %>
<%= f.label :title %>
<%= f.text_field :title %>

<%= f.label :description %>
<%= f.text_area :description %>

<%= f.label :cost %>
<%= f.text_field :cost %>

<%= f.submit %>

<% end %>
