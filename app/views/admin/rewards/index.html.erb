<h1>Rewards Index</h1>
<ul>
  <% @rewards.each do |reward| %>
    <li><%= link_to reward.title, admin_reward_path(reward) %></li>
  <% end %>
</ul>

<%= link_to "Create New Reward", new_admin_reward_path %>
